syntax = "proto3";

package it.lysz210.akasha.capacnan.quipus.credentials;

option java_package = "it.lysz210.akasha.capacnan.quipus.credentials";
option java_multiple_files = true;

enum EncryptionAlgo {
  NONE = 0; // No enc
  BASE_64 = 1;
  AES_GCM_128 = 2;
  AES_GCM_256 = 3;
  CHACHA20_POLY1305 = 4;
}

message EncryptionMeta {
  EncryptionAlgo algo = 1;
  bytes nonce = 2;
  string key_id = 3;
}